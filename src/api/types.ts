// ABS (AudiobookShelf) API response shapes now live in @hearthshelf/core, the
// shared package consumed by every HearthShelf surface. This file re-exports
// them so existing `@/api/types` importers keep working unchanged.
export * from '@hearthshelf/core'
