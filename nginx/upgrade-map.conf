# Maps the Upgrade request header to the right Connection value so websocket
# requests upgrade while ordinary requests keep a normal keep-alive connection.
# Lives at http-block scope (conf.d/), consumed by abs_proxy.conf. A static
# `Connection: upgrade` on every request would break non-websocket proxying.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
